package com.test;

public class Starter {

	public static void main(String[] args) {
		// task:
		// Main should launch a bunch of process per the configuration file.
		// it should provide a communication mechanism to the process laucnhed.
		// It should monitor those processes. If some of them goes down it should 
		// try to restart them. If there are dependencies then it should kill them 
		// before starting them.
		
		// Should also have a UI to monitor the health of process, start, stop processes.
		// GUI frameworks: javaFX, swing, JWT, Qt Jambi etc.
	}

}
